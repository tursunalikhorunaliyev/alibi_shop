import 'package:flutter/material.dart';

class SpecialForUPart extends StatelessWidget {
  const SpecialForUPart({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
